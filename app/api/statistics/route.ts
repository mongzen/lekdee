import {providerFetch} from '@/lib/provider';
export async function GET(){try{const data=await providerFetch('LOTTERY_RESULTS_API_URL');if(!data)return Response.json({mode:'unconfigured',items:[]});if(!Array.isArray(data.results)||typeof data.sourceUrl!=='string'||!data.sourceUrl.startsWith('https://'))throw new Error('Invalid data');const seen=new Set<string>();const results=data.results.slice(0,5000).map((r:{date:string;lastTwo:string})=>{if(!/^\d{4}-\d{2}-\d{2}$/.test(r.date)||!/^\d{2}$/.test(r.lastTwo)||seen.has(r.date))throw new Error('Invalid record');seen.add(r.date);return r;});const counts=new Map<string,{number:string;count:number;lastSeen:string}>();for(const r of results){const item=counts.get(r.lastTwo)||{number:r.lastTwo,count:0,lastSeen:r.date};item.count++;if(r.date>item.lastSeen)item.lastSeen=r.date;counts.set(r.lastTwo,item)}return Response.json({mode:'live',draws:results.length,sourceUrl:data.sourceUrl,items:[...counts.values()].sort((a,b)=>b.count-a.count)});}catch{return Response.json({error:'ไม่สามารถตรวจสอบข้อมูลสถิติได้'},{status:502})}}
